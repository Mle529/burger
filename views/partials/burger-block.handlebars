<div class="container" style="margin-bottom: 20px">
    <div class="row">
        <div class="col" style="background-color: whitesmoke">
            {{id}}.
            {{burger_name}}
        </div>
        <div class="col">
            <button class="devoured-state" data-id="{{id}}" data-newdevour="{{devoured}}"
                style="border-radius: 5px; color: whitesmoke; background-color:tomato; font-family:'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif; font-size: 15px">
                {{#if devoured}}Taste Great! {{else}}Bring it Back!{{/if}}
            </button>
        </div>
    </div>
</div>