<h1
    style="color: rgb(31, 30, 30); text-align: center; font-family:'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif">
    Burger Time</h1>
<br>
<div class="container"
    style="font-family:'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif; font-size: 15px; font-weight:bolder">
    <div class="row">
        <div class="col">
            {{#each burgers}}
            {{#unless devoured}}
            {{> burger-block devoured=true}}
            {{/unless}}
            {{/each}}
        </div>
        <div class="col">
            {{#each burgers}}
            {{#if devoured}}
            {{> burger-block devoured=false}}
            {{/if}}
            {{/each}}
        </div>
    </div>
</div>
<br>
<div
    style=" color: white, font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif; text-align:center">
    <form class="create-form">
        <div class="form-group">
            <label for="newBurger"></label>
            <input type="text" id="newBurger" name="name" style="height: 50px; width: 300px">
        </div>
        <div class="form-group" style="margin-top: -20px">
            <label for="devoured"></label> <br>
            <input type="radio" name="devoured" value="1" checked> Devoured! <br>
            <input type="radio" name="devoured" value="0"> Save for later
        </div>
        <button type="submit"
            style=" border-radius: 5px; color: black; background-color:rgb(245, 226, 53); height: 40px; width: 150px">Click
            Here</button>
        <div style="padding: 10px; font-weight:bold; font-size: 15px; margin-top: 10px"> What burger would you like to
            eat?
        </div>
    </form>
</div>